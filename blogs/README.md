# Blogs

This folder now contains the blog landing page for the site.

Current structure:

- `index.html` - main blog listing page
- root-level post files such as `../firstblog.html`, `../techfest.html`, and `../Diwali.html`

This keeps existing post URLs working while giving the site a cleaner blog hub at `/blogs/`.
